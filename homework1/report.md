#  41343150
第一題
## 作業一 Ackermann 函數

## 解題說明
本題要求實現一個用遞迴函式來計算 Ackermann 函式 A(m,n) 的值。

###  解題策略
1. 當 m = 0 時，返回 n + 1，作為遞迴的結束條件之一。  
2. 當 n = 0 時，返回 A(m - 1, 1)。  
3. 否則，返回 A(m - 1, A(m, n - 1))，持續遞迴直到滿足結束條件。  

## 程式實作
```cpp
#include <iostream>
using namespace std;

int ack(int m, int n) {
    if (m == 0) {
        return n + 1;
    } else if (n == 0) {
        return ack(m - 1, 1);
    } else {
        return ack(m - 1, ack(m, n - 1));
    }
}

int main() {
    int m, n;
    cout << "Enter m and n: ";
    cin >> m >> n;
    if (m < 0 || n < 0) {
        cout << "輸入錯誤：m 和 n 必須是 >= 0 的整數。" << endl;
        return 1;
    }
    cout << "Ackermann(" << m << ", " << n << ") = " << ack(m, n) << endl;
    return 0;
}
````

## 效能分析

程式的運算量隨 Ackermann 函數快速增長，時間複雜度約為 O(A(m,n))，遞迴深度也與 A(m,n) 成正比。  
示意圖如下:  
| \( m \) 值 | 時間複雜度     | 空間複雜度   |
| --------- | -------------- | ------------ |
| \( m = 0 \) | \( O(1) \)      | \( O(1) \)     |
| \( m = 1 \) | \( O(n) \)      | \( O(n) \)     |
| \( m = 2 \) | \( O(n) \)      | \( O(n) \)     |
| \( m = 3 \) | \( O(2^n) \)    | \( O(2^n) \)   |
| \( m \geq 4 \) | \( O(A(m, n)) \) | \( O(A(m, n)) \) |

## 測試與驗證

| 測試案例 | 輸入參數 m, n | 預期輸出   | 實際輸出   |
| ---- | --------- | ------ | ------ |
| 測試一  | 0, 0      | 1      | 1      |
| 測試二  | 1, 1      | 3      | 3      |
| 測試三  | 2, 3      | 9      | 9      |
| 測試四  | 3, 2      | 29     | 29     |
| 測試五  | -1, 2     | 輸入錯誤提示 | 輸入錯誤提示 |

### 編譯與執行指令

```shell
g++ -std=c++20 -o ackermann ackermann.cpp
./ackermann
```

### 結論

程式能正確遞迴計算 Ackermann 函數 A(m, n) 的值，並能處理 m < 0 或 n < 0 的錯誤輸入。  
測試案例涵蓋邊界與非定義情境，驗證遞迴正確性與輸入防護。

## 申論及開發報告
程式使用遞迴來計算 Ackermann 函數，結構簡單且直觀。  
每次遞迴調用都處理一個子問題，並在最後逐層返回結果，這樣可以很清楚地展現遞迴的運作原理和函式堆疊的過程。  
程式碼容易理解，還能有效處理深層的遞迴調用。
